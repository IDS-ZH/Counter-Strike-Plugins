// zh_gravity - gravity settings per mode/team
"zh_gravity"
{
	// Global gravity scale (1.0 = default 800). Applied when plugin enabled.
	"global_scale"	"1.0"

	// Optional per-team overrides (leave blank to ignore).
	"ct_scale"		""
	"t_scale"		""
}
